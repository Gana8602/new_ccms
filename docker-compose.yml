version: '3.8'

services:
  django_app:
    image: ghcr.io/gana8602/new_ccms:latest
    container_name: django_app
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./media:/app/media
      - ./staticfiles:/app/staticfiles
